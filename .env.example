# Secrets for local smoke-tests of api/*.js (never commit real values).
# On Vercel, set the same names in Project → Settings → Environment Variables.
PURPLEAIR_API_KEY=
# Optional override; default is hard-coded in api/purpleair.js for this neighborhood.
PURPLEAIR_SENSOR_INDEX=
TEMPEST_TOKEN=
# Optional override; default is hard-coded in api/tempest.js for this neighborhood.
TEMPEST_STATION_ID=
